#include <vector>
using namespace std;



vector<int> subarraySum(vector<int> arr, int s){
    int currentSearchLength = arr.size();
    bool stat = false;

    while (currentSearchLength--) {
        
    }
}